.vscode
algencan.out
algencan_hsl_accel_cutest.txt
cutest_selection.old
cutest_selection.txt
Manifest.toml
docs/build

# Maintainer notes on the HSL work. Kept locally, deliberately not tracked, so
# that a temporary working document does not ship in a registered release.
contrib/HSLstatus.md
